int [] 
function FindLargest(numbers)
{	
	int largest = numbers[0];
	for(int i = 1 ; i < strlen(numbers) - 1; i++)
	{	
		if(numbers[i] > largest)
		{
			largest = numbers[i];
		}
	}
return largest;
}	
		
